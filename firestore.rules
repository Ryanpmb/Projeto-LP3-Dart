rules_version = '2';

// ATENÇÃO: regras permissivas destinadas APENAS ao Firebase Emulator e aos
// testes de integração locais. NÃO use isto em produção.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
